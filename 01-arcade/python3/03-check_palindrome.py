"""
Given the string, check if it is a palindrome.

Example: 
For inputString = "aabaa", the output should be check_palindrome(inputString) = true;
For inputString = "abac", the output should be check_palindrome(inputString) = false;
For inputString = "a", the output should be check_palindrome(inputString) = true.
"""

# Imports that are automatically included:
import json
import math
import string
import re
import random
import sys
from tempfile import TemporaryFile
import traceback
import functools
from collections import OrderedDict

import numpy
import sortedcontainers

def check_palindrome(inputString):
    """ 
    First attempt: Given the string, check if it is a palindrome.
    """
    return inputString

"""

"""

def check_palindrome(inputString):
    """ 
    Second Attempt: Given the string, check if it is a palindrome.
    """
    return inputString == inputString[::-1]

"""
Again I knew there was a better way to solve this problem, but I forget the syntax for reversing a string
using indexing
"""

def check_palindrome(year):
    """ 
    Rigorous Solution: Given a non-empty string consisting of lowercase characters
            return a boolean True if it is a palindrome and False otherwise.
    -INPUT-
    inputString: 1 <= inputString.length <= 10^5
    -OUTPUT-
    boolean
    """
    try:
        return
    except:
        return 
"""
process explanation for rigorous_solution
"""